import 'package:flutter/material.dart';

class ScaffoldAndTabbarWidget extends StatelessWidget {

  @override Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('Scaffold、TabBar、底部导航'),
      ),
      body: Column(
        children: <Widget>[

        ],
      ),
    );
  }
}